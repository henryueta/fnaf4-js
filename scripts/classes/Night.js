
class Night {

    constructor(config){

        this.number = config.number;
        this.event_running_interval = config.event_running_interval;
        this.isCompleted = config.isCompleted;

    }

}

export {
    Night
}